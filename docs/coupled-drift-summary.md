# Coupled-drift summary

The branch adds one exact rational path optimizer, one exact sparse dual receipt, one bounded precommitted code-sequence optimizer, one independent two-state grid checker, and explicit tests for a strict temporal coupling gap and a code-switching phase transition.
