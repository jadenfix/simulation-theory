# Proposed PR summary

This branch adds exact finite-horizon coupled source-law paths with time-varying
costs, rational primal/dual receipts, marginal compatibility gaps,
precommitted zero-error codebook sequences, switching penalties, exact binary
and K3 examples, typed claims, exhaustive tests, and full project integration.

The load-bearing boundary is that code sequences are open loop. Observation-
based adaptation is deferred to a separate branch with an explicit information
pattern.
