# Coupled-drift PR check

The PR should be opened from `coupled-drift-code-sequences-v2` into `main` only
after the latest branch-head workflow succeeds. It should be squash merged using
the exact reviewed head SHA.
