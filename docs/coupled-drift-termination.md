# Coupled-drift branch termination condition

No additional theorem is added to this branch after the exact coupled path,
dual, marginal-gap, and precommitted-sequence results. Further work belongs in a
new adaptive-policy branch so that the information pattern is reviewed
separately.
