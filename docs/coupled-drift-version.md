# Coupled-drift theorem version

This branch introduces the first repository version of exact coupled finite-horizon TV path optimization and precommitted code-sequence switching. Later adaptive-control work must cite this model as the open-loop baseline rather than mutating its assumptions in place.
